# nacos server 地址
spring:
  application:
    name: member
  cloud:
    nacos:
      config:
        file-extension: yaml
        namespace: public
        server-addr: 127.0.0.1:8858
      discovery:
        namespace: pubic
        server-addr: 127.0.0.1:8858
  profiles:
    active: dev
test:
  nacos: hello========